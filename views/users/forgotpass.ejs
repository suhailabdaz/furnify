<!DOCTYPE html>
<html>
<head>
<title>OTP Verification</title>
<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f0f0f0;
        text-align: center;
        padding: 20px;
    }

    h1 {
        color: #333;
    }

    .container {
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        margin: 0 auto;
        max-width: 300px;
    }

    form {
      margin-top: 20px;
    }

    label {
      display: block;
      text-align: left;
      margin-top: 10px;
    }

    input[type="text"] {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    button {
      width: 100%;
      background-color: #000000;
      color: #fff;
      border: none;
      padding: 10px;
      border-radius: 5px;
      cursor: pointer;
    }

    p {
      color: #333;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Forgot Password ?</h1>

    <form method="post" action="/forgotpasspost">
      <label for="otp">Enter Email</label>
      <input type="email" id="email" name="email" required>
      <% if(locals.emaile){%>
        <div class="alter text-danger text-start"><%= emaile%></div>

      <%}%>
      <button type="submit">Submit</button>
    </form>
  </div>
</body>
</html>
