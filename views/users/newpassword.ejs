<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #000;
            color: #fff;
            text-align: center;
            padding: 20px;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .container {
            background-color: #000;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.1);
            padding: 20px;
            width: 300px;
        }

        h1 {
            color: #fff;
            font-size: 24px;
            margin-bottom: 20px;
        }

        form {
            margin-top: 20px;
        }

        label {
            display: block;
            text-align: left;
            margin-top: 10px;
            color: #aaa;
            font-size: 14px;
        }

        input[type="password"] {
            width: calc(100% - 20px);
            padding: 10px;
            margin: 8px 0;
            border: 1px solid #fff;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 14px;
            color: #fff;
            background-color: transparent;
            outline: none;
        }

        button {
            width: calc(100% - 20px);
            background-color: #000;
            color: #fff;
            border: none;
            padding: 10px;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        button:hover {
            background-color: #444;
            color: #fff;
        }

        .alert {
            color: #ff0000;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Reset Your Password</h1>
        <form method="POST" action="/resetpassword">
            <!-- New Password -->
            <label for="new-password">New Password:</label>
            <input type="password" id="new-password" name="newPassword" required>
            <% if(expressFlash.perror){ %>
                <div class="alert"><%= expressFlash.perror %></div>
            <% } %>

            <!-- Confirm Password -->
            <label for="confirm-password">Confirm Password:</label>
            <input type="password" id="confirm-password" name="confirmPassword" required>
            <% if(expressFlash.cperror){ %>
                <div class="alert"><%=expressFlash.cperror %></div>
            <% } %>

            <button type="submit">Reset Password</button>
        </form>
    </div>
</body>
</html>
